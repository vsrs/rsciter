[package]
name = "rsciter_macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full"] }
proc-macro2 = "1.0"
quote = "1.0"
proc-macro-error = "1.0"

[dev-dependencies]
trybuild = "1"
rsciter = { path = "../rsciter" }